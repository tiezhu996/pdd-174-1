做一个命令行环境变量检测工具。

运行后扫描当前系统中所有已设置的环境变量，按分类整理展示，比如路径类、认证类、开发工具类、系统类等。对常见的开发环境变量，检查值是否正确，比如检测 node 版本路径是否存在、数据库连接字符串格式是否合法。

支持导出环境变量快照，保存成文件方便在新机器上恢复。能对比当前环境和之前保存的快照，列出新增、修改和删除的变量。敏感信息如密码和密钥自动打码显示。支持一键复制某个变量的值到剪贴板。可以限定只显示包含某些关键词的变量。
